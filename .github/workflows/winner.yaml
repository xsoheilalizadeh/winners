name: Pick a Random Winner

on:
  workflow_dispatch:

jobs:
  pick-winner:
    runs-on: ubuntu-latest

    steps:
    - name: 🎉 Pick a Winner
      run: |
        WINNER=$(jq -r '. | .[rand | floor]' usernames.json)
        echo "🏆 Winner: $WINNER"
      env:
        rand: $RANDOM
